/**
 * Fallback when the app cannot reach the API (e.g. offline). Live data comes from
 * GET /api/gallery (Uploadcare CDN, admin uploads at /admin).
 */
export const GALLERY_FILE_NAMES = [];

export const GALLERY_WORK_IMAGES = [];

export const GALLERY_HOME_PREVIEW_COUNT = 7;
